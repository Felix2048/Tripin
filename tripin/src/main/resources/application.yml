spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/tripin
    username: root
    password: tripin

mybatis:
  type-aliases-package: com.tripin..application.entity